<Page x:Class="BE_Cruncher.Pages.VersionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Versions"
      d:DesignWidth="720" d:DesignHeight="500">
    <DockPanel Margin="10">
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
            <Button x:Name="SettingsButton" DockPanel.Dock="Right" Content="Settings" Width="90" Click="SettingsButton_Click"/>
            <TextBlock Text="Step 1: Choose a Battery Emulator version" FontWeight="Bold" VerticalAlignment="Center"/>
        </DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="RefreshButton" Content="Refresh" Width="100" Click="RefreshButton_Click"/>
            <Button x:Name="DownloadButton" Content="Download Selected" Width="150" Margin="10,0,0,0" IsEnabled="False" Click="DownloadButton_Click"/>
            <Button x:Name="AnalyzeButton" Content="Analyze Selected" Width="150" Margin="10,0,0,0" IsEnabled="False" Click="AnalyzeButton_Click"/>
            <Button x:Name="ConfigureButton" Content="Configure Build &#8594;" Width="150" Margin="10,0,0,0" IsEnabled="False" Click="ConfigureButton_Click"/>
        </StackPanel>
        <TextBlock DockPanel.Dock="Top" x:Name="StatusText" Margin="0,0,0,10" TextWrapping="Wrap"/>
        <ListView x:Name="ReleasesList" SelectionChanged="ReleasesList_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Version" Width="240" DisplayMemberBinding="{Binding DisplayName}"/>
                    <GridViewColumn Header="Published" Width="110" DisplayMemberBinding="{Binding Published}"/>
                    <GridViewColumn Header="Prerelease" Width="80" DisplayMemberBinding="{Binding Prerelease}"/>
                    <GridViewColumn Header="Status" Width="220" DisplayMemberBinding="{Binding Status}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Page>
