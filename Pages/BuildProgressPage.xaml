<Page x:Class="BE_Cruncher.Pages.BuildProgressPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Build Progress"
      d:DesignWidth="720" d:DesignHeight="500">
    <DockPanel Margin="10">
        <TextBlock DockPanel.Dock="Top" x:Name="HeaderText" Text="Step 3: Building" FontWeight="Bold" Margin="0,0,0,10"/>
        <TextBlock DockPanel.Dock="Top" x:Name="SummaryText" Text="" FontWeight="SemiBold"
                   Foreground="{StaticResource AccentBrush}" Margin="0,0,0,4" TextWrapping="Wrap"/>
        <TextBlock DockPanel.Dock="Top" x:Name="StatusText" Text="Starting..." Margin="0,0,0,10" TextWrapping="Wrap"
                   Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
        <TextBox x:Name="LogBox"
                 IsReadOnly="True"
                 FontFamily="Consolas"
                 FontSize="12"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"/>
    </DockPanel>
</Page>
