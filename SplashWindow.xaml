<Window x:Class="BE_Cruncher.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="BE Cruncher"
        Width="420" Height="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="True"
        Icon="/Images/app.ico">
    <Border Background="{StaticResource Bg0Brush}" BorderBrush="{StaticResource BorderBrush2}"
            BorderThickness="1" CornerRadius="12">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Image Grid.Row="0" Source="/Images/logo-cropped.png" Stretch="Uniform" Margin="0,0,0,10"/>

            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,10,0,0">
                <TextBlock x:Name="StatusText" Text="Starting..." Foreground="{StaticResource TextMutedBrush}"
                           HorizontalAlignment="Center" FontSize="12"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
